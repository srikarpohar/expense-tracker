{
  "name": "expense-tracker",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "et-web": "pnpm --filter \"et-web\" run",
    "et-backend": "pnpm --filter \"et-backend\" run",
    "et-types": "pnpm --filter \"expense-tracker-types\" run",
    "et-utils": "pnpm --filter \"expense-tracker-utils\" run",
    "backend-dev": "docker compose up -d backend-dev",
    "frontend-dev": "docker compose up -d frontend-dev",
    "app-build": "docker compose build backend-dev frontend-dev",
    "app-dev": "docker compose up -d backend-dev frontend-dev",

    "backend-build:prod": "docker compose up -d backend-prod",
    "web-deploy": "vercel --local-config ./vercel.json",
    "web-deploy:prod": "vercel --prod --local-config ./vercel.json"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.20.0",
  "dependencies": {
    "vue": "^3.5.24"
  }
}
